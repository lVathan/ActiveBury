import os
from flask import Flask, render_template, url_for, json

def event_reader():
    events=
    {
     title: 'Wake up',
     start: moment().format('L'),
     },
     {
     title: 'Go to bed',
     start: moment().format('L'),
     }
    return events

events = event_reader()
